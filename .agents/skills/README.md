# GSV VDT skills (git canon)

Canon for shared Agent Skills is **this directory**. Edit here first, then:

```bash
bash scripts/sync-vdt-skill-mirrors.sh
```

Client copies (must stay identical on Windows — copy, not symlink):

- `.cursor/skills/`
- `.opencode/skills/`

Do **not** copy PoolAI product skills (`poolai-documentation`). Product docs live in the product repo.

Kit split: [`docs/gsv/GSV_VDT_KIT.md`](../../docs/gsv/GSV_VDT_KIT.md) · registry: [`docs/gsv/PRODUCTS.md`](../../docs/gsv/PRODUCTS.md).
