# `bin/`

Product wrappers used to live here as `.sh`. They are **Rust** now:

```
cargo xtask record-speed
cargo xtask record-rust
cargo xtask sync
```

See [`scripts/README.md`](../scripts/README.md) and [`docs/gsv/GSV_RUST_DEV.md`](../docs/gsv/GSV_RUST_DEV.md).
